// top-5 entries () in javascript.
const arr = ["Abhimanyu", "Ashu", "Amit", "Abhinas"];

let iterator = arr.entries();

for (let newIterator of iterator) {
  console.log(newIterator);
}

// second examples.
const arrTwo = [1, 2, 3, 4, 5, 6, 7];

const newIterate = arrTwo.entries();

for (let newIte of newIterate) {
  console.log(newIte);
}
